/* eslint-disable no-unused-vars */
export enum ENUM_CANCELLATION_REQUEST_STATUS {
    PENDING = 'PENDING',
    ACCEPTED = 'ACCEPTED',
    REJECTED = 'REJECTED',
    RESOLVED = 'RESOLVED',
    DISPUTED = 'DISPUTED',
}
