/* eslint-disable no-unused-vars */
export enum ENUM_DISCOUNT_TYPE {
    PERCENT = 'PERCENT',
    FIXED = 'FIXED',
}

export enum ENUM_PROMO_STATUS {
    ACTIVE = 'ACTIVE',
    UPCOMING = 'UPCOMING',
    EXPIRED = 'EXPIRED',
}
